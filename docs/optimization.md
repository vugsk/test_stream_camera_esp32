# Оптимизация производительности

Данный документ содержит рекомендации по оптимизации ESP32-CAM для достижения максимальной производительности.

---

## ⚡ Оптимизация аппаратной части

### 1. Питание

**Проблема**: Нестабильное питание вызывает перезагрузки и потерю кадров.

**Решение**:
- Используйте качественный блок питания 5V/2A минимум
- Добавьте конденсатор 100µF на линии питания
- Избегайте питания через USB-TTL адаптер при стриминге

```
                 ESP32-CAM
                    │
    5V ───[100µF]───┤
                    │
   GND ─────────────┤
```

### 2. WiFi антенна

**Встроенная антенна**: Дальность до 10-20м

**Внешняя антенна**: 
- Используйте разъем U.FL/IPEX
- Увеличивает дальность до 50-100м
- Улучшает стабильность сигнала

**Настройка**:
```cpp
// Переключение на внешнюю антенну
WiFi.setTxPower(WIFI_POWER_19_5dBm);  // Максимальная мощность
```

### 3. Охлаждение

При длительной работе ESP32 нагревается:
- Добавьте радиатор на чип ESP32
- Обеспечьте вентиляцию
- Избегайте работы в закрытых корпусах без вентиляции

---

## 🚀 Оптимизация прошивки

### 1. CPU частота

```cpp
void setup() {
    // Максимальная частота для лучшей производительности
    setCpuFrequencyMhz(240);  // 240 MHz
    
    // Проверка
    Serial.printf("CPU: %d MHz\n", getCpuFrequencyMhz());
}
```

**Варианты**:
- 240 MHz — максимальная производительность
- 160 MHz — баланс производительность/энергопотребление
- 80 MHz — низкое энергопотребление (не рекомендуется для HD)

### 2. WiFi оптимизация

```cpp
bool initWiFi() {
    WiFi.mode(WIFI_STA);
    
    // Отключение sleep для минимальных задержек
    WiFi.setSleep(false);
    
    // Максимальная мощность передатчика
    WiFi.setTxPower(WIFI_POWER_19_5dBm);
    
    WiFi.begin(ssid, password);
    
    // После подключения
    esp_wifi_set_ps(WIFI_PS_NONE);  // Disable power save
    
    return true;
}
```

### 3. Камера оптимизация

```cpp
camera_config_t config;

// Двойная буферизация для плавного захвата
config.fb_count = 2;

// PSRAM обязателен для HD разрешений
config.fb_location = CAMERA_FB_IN_PSRAM;

// Пропуск устаревших кадров (важно!)
config.grab_mode = CAMERA_GRAB_LATEST;

// Максимальная частота XCLK
config.xclk_freq_hz = 20000000;  // 20 MHz
```

### 4. TCP оптимизация

```cpp
WiFiClient client;

// Отключение алгоритма Nagle для минимальных задержек
client.setNoDelay(true);

// Короткие таймауты
client.setTimeout(500);

// Большие чанки для быстрой передачи
const size_t CHUNK_SIZE = 16384;  // 16KB
```

---

## 📊 Оптимальные настройки для разных сценариев

### Высокое качество (архивирование)

```cpp
frameSize = FRAMESIZE_UXGA;  // 1600x1200
quality = 10;
fps = 10;
```

**Производительность**:
- Размер кадра: ~150 KB
- Bitrate: ~12 Mbps
- CPU load: 70%

### HD стриминг (мониторинг)

```cpp
frameSize = FRAMESIZE_HD;  // 1280x720
quality = 15;
fps = 30;
```

**Производительность**:
- Размер кадра: ~90 KB
- Bitrate: ~22 Mbps
- CPU load: 85%

### Низкая задержка (real-time)

```cpp
frameSize = FRAMESIZE_VGA;  // 640x480
quality = 20;
fps = 60;
```

**Производительность**:
- Размер кадра: ~20 KB
- Bitrate: ~10 Mbps
- CPU load: 60%

### Медленные сети

```cpp
frameSize = FRAMESIZE_QVGA;  // 320x240
quality = 30;
fps = 15;
```

**Производительность**:
- Размер кадра: ~5 KB
- Bitrate: ~0.6 Mbps
- CPU load: 30%

---

## 🔧 Продвинутые оптимизации

### 1. Динамическая адаптация качества

```cpp
void adjustQualityBasedOnNetwork() {
    int rssi = WiFi.RSSI();
    
    if (rssi > -50) {
        // Отличный сигнал
        setStreamFPS(60);
        currentSettings.quality = 15;
    } else if (rssi > -70) {
        // Хороший сигнал
        setStreamFPS(30);
        currentSettings.quality = 20;
    } else {
        // Слабый сигнал
        setStreamFPS(15);
        currentSettings.quality = 30;
    }
    
    applyCameraSettings(currentSettings);
}
```

### 2. Приоритизация задач

```cpp
void loop() {
    // 1. Высший приоритет: отправка кадров
    updateStreaming();
    
    // 2. Критичное: проверка WiFi (но не во время Bluetooth)
    if (connectionState != STATE_BLUETOOTH_WAITING) {
        checkWiFiConnection();
    }
    
    // 3. Низкий приоритет: получение настроек (каждые 5 сек)
    handleServerSettings();
    
    // 4. Самый низкий: отправка статуса (каждые 30 сек)
    if (millis() - lastStatusTime > 30000) {
        sendStatusToServer();
        lastStatusTime = millis();
    }
    
    // Без delay() для максимальной пропускной способности
}
```

### 3. Оптимизация памяти

```cpp
// Использование статической памяти вместо динамической
static char httpHeader[256];  // Pre-allocated buffer
static String serverHost;     // Cached server address

// Освобождение буферов сразу после использования
camera_fb_t* fb = captureFrame();
sendFrameData(fb);
releaseFrame(fb);  // Немедленное освобождение
```

### 4. RTOS приоритеты (продвинутое)

```cpp
// Создание отдельной задачи для стриминга
void streamingTask(void* parameter) {
    while (true) {
        if (isStreaming()) {
            sendFrame();
        }
        vTaskDelay(1);  // Yield to other tasks
    }
}

void setup() {
    // ... основная инициализация
    
    // Создание задачи с высоким приоритетом
    xTaskCreatePinnedToCore(
        streamingTask,    // Функция
        "Streaming",      // Имя
        10000,            // Stack size
        NULL,             // Parameter
        2,                // Priority (0-24, выше = важнее)
        NULL,             // Task handle
        1                 // Core ID (0 или 1)
    );
}
```

---

## 📡 Оптимизация сети

### 1. Router настройки

**QoS (Quality of Service)**:
```
Приоритет: HIGH
Protocol: TCP
Port: 8081
Device: ESP32-CAM MAC address
```

**Channel Selection**:
- Используйте WiFi Analyzer для выбора свободного канала
- Избегайте перекрытия с соседними сетями
- Рекомендуемые каналы: 1, 6, 11

### 2. Сетевая топология

**Оптимально**:
```
ESP32-CAM ──► Router ──► Server (LAN)
  WiFi          Ethernet
```

**Неоптимально**:
```
ESP32-CAM ──► Router ──► Internet ──► Cloud Server
  WiFi          Высокая задержка
```

### 3. Bandwidth calculation

| Разрешение | FPS | Quality | Размер кадра | Bitrate | WiFi нагрузка |
|------------|-----|---------|--------------|---------|---------------|
| QVGA | 30 | 30 | 5 KB | 1.2 Mbps | 10% |
| VGA | 30 | 20 | 20 KB | 4.8 Mbps | 30% |
| HD | 30 | 15 | 90 KB | 22 Mbps | 90% |
| UXGA | 10 | 10 | 150 KB | 12 Mbps | 50% |

**WiFi теоретический максимум**: ~50 Mbps (реально ~30 Mbps)

---

## 🔍 Профилирование

### Измерение производительности

```cpp
// Таймер для измерения времени выполнения
unsigned long startTime = micros();

// Ваш код
sendFrame();

unsigned long elapsed = micros() - startTime;
Serial.printf("sendFrame() took: %lu µs\n", elapsed);
```

### Мониторинг памяти

```cpp
void printMemoryStats() {
    Serial.printf("Free heap: %d bytes\n", ESP.getFreeHeap());
    Serial.printf("Free PSRAM: %d bytes\n", ESP.getFreePsram());
    Serial.printf("Min free heap: %d bytes\n", ESP.getMinFreeHeap());
}
```

### Анализ WiFi

```cpp
void printWiFiStats() {
    Serial.printf("RSSI: %d dBm\n", WiFi.RSSI());
    Serial.printf("Channel: %d\n", WiFi.channel());
    Serial.printf("TX Power: %d\n", WiFi.getTxPower());
}
```

---

## ⚠️ Типичные проблемы и решения

### Проблема: Низкий FPS

**Причины**:
- Слабый WiFi сигнал
- Медленный сервер
- Переполнение буфера

**Решения**:
1. Уменьшите разрешение или качество
2. Проверьте RSSI: `WiFi.RSSI()` (> -70 dBm хорошо)
3. Оптимизируйте сервер (async обработка)

### Проблема: Задержки (lag)

**Причины**:
- WiFi Power Save включен
- TCP буфер переполнен
- Медленная обработка на сервере

**Решения**:
```cpp
WiFi.setSleep(false);
esp_wifi_set_ps(WIFI_PS_NONE);
client.setNoDelay(true);
```

### Проблема: Перезагрузки (brownout)

**Причины**:
- Недостаточное питание
- Перегрев

**Решения**:
- Используйте БП 5V/2A минимум
- Добавьте конденсатор 100µF
- Обеспечьте охлаждение

### Проблема: Артефакты на изображении

**Причины**:
- Низкое качество JPEG
- Помехи питания
- Плохой кабель камеры

**Решения**:
- Увеличьте quality (уменьшите число)
- Улучшите питание
- Проверьте подключение камеры

---

## 📈 Бенчмарки

### Реальные показатели (AI-Thinker ESP32-CAM)

**Тест 1: VGA @ 60 FPS**
```
Resolution: 640x480
Quality: 20
Average FPS: 58
Frame size: ~22 KB
Network: -45 dBm WiFi
CPU load: 65%
```

**Тест 2: HD @ 30 FPS**
```
Resolution: 1280x720
Quality: 15
Average FPS: 29
Frame size: ~85 KB
Network: -45 dBm WiFi
CPU load: 82%
```

**Тест 3: UXGA @ 10 FPS**
```
Resolution: 1600x1200
Quality: 10
Average FPS: 9.8
Frame size: ~145 KB
Network: -45 dBm WiFi
CPU load: 68%
```

---

## 📚 Дополнительные ресурсы

- [ESP32 Performance Optimization](https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-guides/performance/index.html)
- [WiFi Performance Guide](https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-guides/wifi.html#esp32-wi-fi-station-and-ap-general-scenario)
- [FreeRTOS Task Priorities](https://www.freertos.org/RTOS-task-priority.html)
