# Конфигурация проекта

## ⚙️ Файл config.h

Основные константы проекта находятся в `include/config.h`.

### Полная конфигурация

```cpp
#ifndef CONFIG_H
#define CONFIG_H

// ==================== Настройки Wi-Fi ====================
#define WIFI_SSID ""          // Имя вашей WiFi сети (по умолчанию пусто)
#define WIFI_PASSWORD ""       // Пароль WiFi (по умолчанию пусто)

// ==================== Настройки сервера ====================
#define SERVER_HOST ""                   // IP вашего веб-сервера
#define SERVER_PORT 8081                 // Порт сервера
#define STREAM_PATH "/stream"            // Путь для отправки видео потока
#define SETTINGS_PATH "/api/camera"      // Путь для получения настроек
#define STATUS_PATH "/api/status"        // Путь для отправки статуса

// ==================== Пины камеры для AI-Thinker ESP32-CAM ====================
#define PWDN_GPIO_NUM     32
#define RESET_GPIO_NUM    -1
#define XCLK_GPIO_NUM      0
#define SIOD_GPIO_NUM     26
#define SIOC_GPIO_NUM     27
#define Y9_GPIO_NUM       35
#define Y8_GPIO_NUM       34
#define Y7_GPIO_NUM       39
#define Y6_GPIO_NUM       36
#define Y5_GPIO_NUM       21
#define Y4_GPIO_NUM       19
#define Y3_GPIO_NUM       18
#define Y2_GPIO_NUM        5
#define VSYNC_GPIO_NUM    25
#define HREF_GPIO_NUM     23
#define PCLK_GPIO_NUM     22

// ==================== Настройки стриминга ====================
#define STREAM_FPS 60                    // Target FPS
#define STREAM_QUALITY 15                // JPEG quality (10-63, lower=better)

#endif // CONFIG_H
```

---

## 📝 Описание параметров

### WiFi настройки

#### `WIFI_SSID`
- **Тип**: String
- **По умолчанию**: `""`
- **Описание**: Имя WiFi сети. Если пусто, настройки будут загружены из NVS или через Bluetooth.

#### `WIFI_PASSWORD`
- **Тип**: String
- **По умолчанию**: `""`
- **Описание**: Пароль WiFi сети.

**Примечание**: Эти значения используются только как fallback. Приоритет отдается настройкам из NVS.

### Серверные настройки

#### `SERVER_HOST`
- **Тип**: String
- **По умолчанию**: `""`
- **Описание**: IP адрес или hostname веб-сервера.
- **Примеры**: 
  - `"192.168.1.100"`
  - `"server.local"`

#### `SERVER_PORT`
- **Тип**: int
- **По умолчанию**: `8081`
- **Описание**: TCP порт веб-сервера.
- **Диапазон**: 1-65535
- **Рекомендации**: Используйте непривилегированные порты (1024-65535)

#### `STREAM_PATH`
- **Тип**: String
- **По умолчанию**: `"/stream"`
- **Описание**: HTTP путь для отправки JPEG кадров.
- **Формат**: Должен начинаться с `/`

#### `SETTINGS_PATH`
- **Тип**: String
- **По умолчанию**: `"/api/camera"`
- **Описание**: HTTP путь для получения настроек камеры.

#### `STATUS_PATH`
- **Тип**: String
- **По умолчанию**: `"/api/status"`
- **Описание**: HTTP путь для отправки статуса устройства.

### Настройки стриминга

#### `STREAM_FPS`
- **Тип**: int
- **По умолчанию**: `60`
- **Описание**: Целевая частота кадров.
- **Диапазон**: 1-60
- **Рекомендации**:
  - VGA (640x480): до 60 FPS
  - HD (1280x720): 30 FPS
  - UXGA (1600x1200): 10-15 FPS

#### `STREAM_QUALITY`
- **Тип**: int
- **По умолчанию**: `15`
- **Описание**: Качество JPEG компрессии.
- **Диапазон**: 10-63 (меньше = лучше качество)
- **Рекомендации**:
  - Высокое качество: 10-15
  - Среднее качество: 15-25
  - Низкое качество: 25-40

---

## 🗄️ NVS (Non-Volatile Storage)

Настройки сохраняются в энергонезависимой памяти ESP32.

### Структура разделов

#### Раздел "wifi"
```cpp
Preferences prefs;
prefs.begin("wifi", false);

// Ключи:
// - "ssid"         (String) - SSID WiFi сети
// - "password"     (String) - Пароль WiFi
// - "server_host"  (String) - Адрес сервера
```

#### Раздел "camera"
```cpp
prefs.begin("camera", false);

// Ключи:
// - "frameSize"    (int)  - Разрешение камеры (5-13)
// - "quality"      (int)  - Качество JPEG (10-63)
// - "brightness"   (int)  - Яркость (-2 до 2)
// - "contrast"     (int)  - Контраст (-2 до 2)
// - "saturation"   (int)  - Насыщенность (-2 до 2)
// - "fps"          (int)  - FPS (1-60)
// - "vflip"        (bool) - Вертикальное отражение
// - "hmirror"      (bool) - Горизонтальное отражение
// - "streaming"    (bool) - Включен стриминг
```

#### Раздел "bluetooth"
```cpp
prefs.begin("bluetooth", false);

// Ключи:
// - "name"         (String) - Имя Bluetooth устройства
// - "enabled"      (bool)   - Включен Bluetooth
```

### Приоритет настроек

1. **NVS** (высший приоритет)
2. **Сервер** (обновляет NVS)
3. **config.h** (fallback если NVS пуст)

---

## 🎛️ Настройки камеры

### Frame Size (Разрешение)

| Код | Константа | Разрешение | FPS (макс) | Размер кадра |
|-----|-----------|------------|------------|--------------|
| 5 | FRAMESIZE_QVGA | 320x240 | 60 | ~10 KB |
| 6 | FRAMESIZE_CIF | 400x296 | 60 | ~15 KB |
| 7 | FRAMESIZE_HVGA | 480x320 | 60 | ~20 KB |
| 8 | FRAMESIZE_VGA | 640x480 | 60 | ~30 KB |
| 9 | FRAMESIZE_SVGA | 800x600 | 30 | ~50 KB |
| 10 | FRAMESIZE_XGA | 1024x768 | 20 | ~70 KB |
| 11 | FRAMESIZE_HD | 1280x720 | 30 | ~90 KB |
| 12 | FRAMESIZE_SXGA | 1280x1024 | 15 | ~120 KB |
| 13 | FRAMESIZE_UXGA | 1600x1200 | 10 | ~150 KB |

### Quality (Качество JPEG)

| Значение | Качество | Размер файла | Использование |
|----------|----------|--------------|---------------|
| 10 | Отличное | 100% | Архивирование, фото |
| 15 | Очень хорошее | 70% | HD стриминг |
| 20 | Хорошее | 50% | VGA стриминг |
| 30 | Среднее | 30% | Мобильные сети |
| 40 | Низкое | 20% | Медленные сети |

### Brightness, Contrast, Saturation

- **Диапазон**: -2 до +2
- **По умолчанию**: 0
- **Шаг**: 1

| Значение | Эффект |
|----------|--------|
| -2 | Минимум |
| -1 | Ниже нормы |
| 0 | Норма |
| +1 | Выше нормы |
| +2 | Максимум |

---

## 📱 Bluetooth конфигурация

### Имя устройства

По умолчанию: `ESP32-CAM-Config`

Изменить можно через сервер:
```json
{
  "bluetooth": {
    "name": "MyCam-01"
  }
}
```

### Форматы данных

#### JSON формат (рекомендуется)
```json
{
  "ssid": "MyWiFi",
  "password": "password123",
  "server_host": "192.168.1.100"
}
```

#### Простой CSV формат
```
MyWiFi,password123,192.168.1.100
```

Формат: `SSID,PASSWORD,SERVER_HOST`

**Примечание**: `server_host` опционален. Если не указан, используется значение из config.h.

---

## 🔧 Расширенные настройки

### platformio.ini

```ini
[env:esp32cam]
platform = espressif32
board = esp32cam
framework = arduino
monitor_speed = 115200

# Партиция для большой прошивки
board_build.partitions = huge_app.csv

# PSRAM поддержка
build_flags = 
    -DBOARD_HAS_PSRAM
    -mfix-esp32-psram-cache-issue

# Библиотеки
lib_deps = 
    ArduinoJson@^7.0.0
```

### Изменение Serial Monitor скорости

В `main.cpp`:
```cpp
Serial.begin(115200);  // Изменить на 9600, 57600, или 230400
```

Соответственно обновить в `platformio.ini`:
```ini
monitor_speed = 115200
```

---

## 🌐 Примеры конфигураций

### Конфигурация для домашней сети

```cpp
// config.h
#define WIFI_SSID "HomeWiFi"
#define WIFI_PASSWORD "mypassword"
#define SERVER_HOST "192.168.1.100"
#define SERVER_PORT 8081
#define STREAM_FPS 30
#define STREAM_QUALITY 15
```

### Конфигурация для офиса

```cpp
// config.h
#define WIFI_SSID "Office-Network"
#define WIFI_PASSWORD "securepass"
#define SERVER_HOST "server.company.local"
#define SERVER_PORT 8080
#define STREAM_FPS 15
#define STREAM_QUALITY 20
```

### Конфигурация для мобильной сети

```cpp
// config.h
#define WIFI_SSID "Mobile-Hotspot"
#define WIFI_PASSWORD "12345678"
#define SERVER_HOST "example.com"
#define SERVER_PORT 80
#define STREAM_FPS 10
#define STREAM_QUALITY 30
```

---

## 🔒 Безопасность

### Рекомендации

1. **Не храните пароли в config.h** — используйте Bluetooth конфигурацию
2. **Используйте сильные пароли WiFi** — минимум 12 символов
3. **Ограничьте доступ к серверу** — используйте firewall
4. **Регулярно обновляйте прошивку**

### Очистка NVS

Для полного сброса настроек:

```cpp
#include <Preferences.h>

Preferences prefs;
prefs.begin("wifi", false);
prefs.clear();  // Удалить все ключи
prefs.end();

prefs.begin("camera", false);
prefs.clear();
prefs.end();
```

Или используйте команду:
```bash
pio run -t erase
```

---

## 📊 Оптимальные настройки

### Для высокого качества (архивирование)
```cpp
frameSize = 13 (UXGA - 1600x1200)
quality = 10
fps = 10
```

### Для плавного стриминга (мониторинг)
```cpp
frameSize = 11 (HD - 1280x720)
quality = 15
fps = 30
```

### Для низкой задержки (реал-тайм)
```cpp
frameSize = 8 (VGA - 640x480)
quality = 20
fps = 60
```

### Для медленных сетей
```cpp
frameSize = 5 (QVGA - 320x240)
quality = 30
fps = 15
```

---

## 🛠️ Отладка конфигурации

### Проверка настроек через Serial Monitor

```cpp
Serial.println("=== Current Configuration ===");
Serial.printf("Server: %s:%d\n", SERVER_HOST, SERVER_PORT);
Serial.printf("FPS: %d\n", STREAM_FPS);
Serial.printf("Quality: %d\n", STREAM_QUALITY);
```

### Проверка NVS настроек

```cpp
Preferences prefs;
prefs.begin("wifi", true);  // read-only
String ssid = prefs.getString("ssid", "not set");
String host = prefs.getString("server_host", "not set");
Serial.println("Saved SSID: " + ssid);
Serial.println("Saved Host: " + host);
prefs.end();
```
